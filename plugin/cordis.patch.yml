# Bundle patch: mount the plugin row. The row name must match this package
# name; remove this file's insert block (or the bundle from the profile
# bundles list) to restore the stock mobile look.
- insert:
    - id: deepseek-mobile
      name: 'ds-mobile-skin'
